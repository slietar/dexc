[build-system]
requires = ["oryn~=1.3"]
build-backend = "oryn"

[project]
name = "dexc-autoinstall"
version = "0.4.0"
description = "Exception formatter"
license = "EUPL-1.2"
readme = "../../readme.md"
requires-python = "~=3.11"
dependencies = [
  "dexc",
]

[project.urls]
Repository = "https://github.com/slietar/dexc"

[tool.oryn]
include = ["dexc_autoinstall.pth"]
